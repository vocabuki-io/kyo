# きょう / とい ─ 設置手順書

はじめて読む人向けに、省略せずに書いてあります。
前に出てきた操作でも、毎回もう一度書きます。

**必要なもの**
- パソコン（GitHubとCloudflareの画面を開くため）
- iPhone
- GitHubのアカウント
- Cloudflareのアカウント

**かかる時間**：はじめて通すと、だいたい40分から1時間。

**全体の流れ**

```
第1章  GitHubに置き場所を作る
第2章  ファイルを置く
第3章  Cloudflareで公開する
第4章  iPhoneのホーム画面に置く
第5章  カレンダーのURLを手に入れる
第6章  自動更新をONにする
第7章  Claudeを繋ぐ（できなくても動きます）
第8章  ショートカットで「とい」を開く
第9章  答えたものをジャーナルに送る（別紙）
```

第6章と第7章は、あとからでもできます。
**第4章まで終われば、手で予定を入れて使えます。**

---

# 第1章 GitHubに置き場所を作る

## 1-1. GitHubを開く

パソコンでブラウザ（Chrome、Safari、Edgeなど）を開きます。

アドレスを入れるところに、次の文字を打ちます。

```
github.com
```

キーボードの `Enter` キーを1回押します。

## 1-2. ログインする

画面の右上に「**Sign in**」という文字があります。1回クリックします。

- メールアドレスまたはユーザー名を入れます
- パスワードを入れます
- 「**Sign in**」ボタンを1回クリックします

すでにログインしている場合は、この操作は要りません。次に進みます。

## 1-3. 新しいリポジトリを作る

「リポジトリ」というのは、ファイルを置く箱のことです。

画面の**右上**に、`＋`（プラス）のマークがあります。1回クリックします。

小さいメニューが下に出てきます。その中の「**New repository**」を1回クリックします。

## 1-4. 名前を決める

「Repository name」と書かれた入力欄があります。そこに次の文字を打ちます。

```
kyou
```

その下のほうに「**Public**」と「**Private**」の2つの丸いボタンがあります。
**Public** の側を1回クリックします。（黒い点が付きます）

> **なぜPublicなのか**
> Cloudflare Pagesは、Publicのほうが設定が少なくて済みます。
> 予定の中身はこの箱には入りません。入るのは、あとで説明する仕組みで
> 自動生成されるファイルだけです。気になる場合は第5章の最後を読んでください。

その下に「Add a README file」というチェックボックスがあります。
**チェックを入れます**（四角をクリックすると、レ点が付きます）。

いちばん下に「**Create repository**」という緑色のボタンがあります。1回クリックします。

## 1-5. できたことを確認する

画面が切り替わって、`あなたの名前 / kyou` と表示されていれば成功です。

---

# 第2章 ファイルを置く

`kyou.zip` を解凍すると、中に次のものが入っています。

```
index.html
prompt.md
prompt-toi.md
tasks.md
README.md
package.json
package-lock.json
.gitignore
_headers
scripts/  という名前のフォルダ
  fetch.mjs
  journal.mjs
  fallback.mjs
  stamp.mjs
  toi.mjs
  version.mjs
.github/  という名前のフォルダ
  workflows/ という名前のフォルダ
    build.yml
    version.yml
data/ という名前のフォルダ
  .gitkeep
  toi.json
```

`data/toi.json` には、といの問いが最初から入っています。
Claudeを繋ぐ（第7章）と、ここが毎日書き換わります。
繋がなくても、入っているぶんは出ます。

これを全部、さっき作った箱に入れます。

## 2-1. アップロードの画面を出す

いま開いている GitHub の `kyou` のページに、
「**Add file**」というボタンがあります。1回クリックします。

小さいメニューが出ます。「**Upload files**」を1回クリックします。

## 2-2. ファイルをドラッグする

画面の真ん中に、点線で囲まれた大きい四角が出ています。

パソコンで、解凍した `kyou` フォルダを開きます。
中のファイルとフォルダを**全部選んで**、点線の四角の中にドラッグします。

（全部選ぶには、Windowsなら `Ctrl` + `A`、Macなら `command` + `A` を押します）

## 2-3. 確定する

画面を下にスクロールします。

「**Commit changes**」という緑色のボタンがあります。1回クリックします。

## 2-4. できたことを確認する

画面が切り替わって、ファイルの一覧が出ます。
`index.html` `scripts` `.github` などが並んでいれば成功です。

> **`.github` フォルダが見当たらないとき**
> `.`（ドット）で始まるフォルダは、パソコン上で見えなくなっていることがあります。
> Windowsなら、フォルダの上のメニューの「表示」→「隠しファイル」にチェック。
> Macなら、フォルダを開いた状態で `command` + `shift` + `.`（ドット）を押します。
> 見えるようになったら、2-1からもう一度やります。

---

# 第3章 Cloudflareで公開する

## 3-1. Cloudflareを開く

ブラウザで新しいタブを開きます。
（タブの右にある `＋` を1回クリックすると、新しいタブが開きます）

アドレスを入れるところに、次の文字を打ちます。

```
dash.cloudflare.com
```

キーボードの `Enter` キーを1回押します。

## 3-2. ログインする

- メールアドレスを入れます
- パスワードを入れます
- 「**Log in**」ボタンを1回クリックします

アカウントを持っていない場合は、「**Sign up**」から作ります。無料です。

## 3-3. Pagesの画面へ行く

画面の**左側**に、メニューが縦に並んでいます。

「**Compute (Workers)**」または「**Workers & Pages**」という文字を探して、1回クリックします。

画面が変わったら、「**Create**」という青いボタンを1回クリックします。

上のほうに「Workers」と「**Pages**」という2つのタブがあります。
**Pages** のほうを1回クリックします。

「**Connect to Git**」という項目があります。その中のボタンを1回クリックします。

## 3-4. GitHubと繋ぐ

「**Connect GitHub**」というボタンが出ます。1回クリックします。

GitHubの画面に移ります。
「**Authorize Cloudflare Pages**」という緑のボタンを1回クリックします。

次に、どのリポジトリを使わせるか選ぶ画面が出ます。
「**Only select repositories**」を選んで、`kyou` を選びます。
「**Install & Authorize**」ボタンを1回クリックします。

## 3-5. リポジトリを選ぶ

Cloudflareの画面に戻ります。
リポジトリの一覧の中から `kyou` を1回クリックします。

右下の「**Begin setup**」ボタンを1回クリックします。

## 3-6. ビルドの設定（ここが大事）

次の画面で、3か所を確認します。

| 項目 | やること |
|---|---|
| Project name | `kyou` のままでいい |
| **Framework preset** | **`None`** を選ぶ |
| **Build command** | **何も入れない（空にする）** |
| **Build output directory** | **何も入れない（空にする）** |

> **なぜ空なのか**
> このアプリは、組み立て作業が要らない作りになっています。
> ここに何か入れると、組み立てようとして失敗します。

いちばん下の「**Save and Deploy**」ボタンを1回クリックします。

## 3-7. 待つ

画面に文字がたくさん流れます。30秒から1分くらい待ちます。

「**Success!**」という文字と、次のようなアドレスが出れば成功です。

```
https://kyou-xxx.pages.dev
```

`xxx` の部分は自動で決まるので、人によって違います。
**このアドレスを、あとで使います。** メモしておいてください。

## 3-8. 開いてみる

そのアドレスを1回クリックします。

黒い画面と、大きい時計が出れば成功です。
下のほうに「とい ↓」という文字があります。

**ここまでで、手入力で使える状態になりました。**

---

# 第4章 iPhoneのホーム画面に置く

## 4-1. Safariで開く

iPhoneで「**Safari**」を開きます。
（Chromeではできません。Safariです）

アドレスを入れるところに、第3章で出たアドレスを打ちます。

```
kyou-xxx.pages.dev
```

キーボードの「**開く**」を1回押します。

## 4-2. ホーム画面に追加する

画面のいちばん下に、四角から矢印が上に出ているマーク（共有ボタン）があります。
1回タップします。

メニューが下から出てきます。指で上にスクロールします。

「**ホーム画面に追加**」という項目を1回タップします。

右上の「**追加**」を1回タップします。

## 4-3. 確認する

ホーム画面に「きょう」というアイコンが増えています。
1回タップして開きます。

上にアドレスバーが出ない、全画面の状態で開けば成功です。

## 4-4. 使ってみる

**予定を1件入れてみます。**

1. 「きょう」の下にある「**＋ 予定を足す**」を1回タップします
2. 「時刻」の欄に `22:00` と打ちます
3. 「なに」の欄に `テスト` と打ちます
4. 「**決定**」を1回タップします

一覧に出れば成功です。

消すときは、その行の右にある「**×**」を1回タップします。

**まとめて入れることもできます。**

1. 画面の右上の「**貼り付け**」を1回タップします
2. 出てきた大きい入力欄に、次のように打ちます

```
8/28 23:45 ボカ戻せ
8/29 15:00 起きる
```

3. 「**決定**」を1回タップします

1行に1件です。`月/日 時:分 なに` の順に書きます。

## 4-5. 「とい」を出してみる

画面のいちばん下に「**とい　↓**」という文字と、短い横棒があります。

1回タップします。（または、その部分を指で下に引きます）

画面が白くなって、質問が出れば成功です。

戻るときは、いちばん上の「**↑　きょう**」を1回タップします。

**ここまでで、アプリとして使えます。**
第5章から先は、カレンダーを自動で取り込むための作業です。

---

# 第5章 カレンダーのURLを手に入れる

ここから、Googleカレンダーの予定を自動で表示させます。

必要なのは、**Motion / ボカブキ / 彼女** の3つのカレンダーです。
（モノトーンは入れません）

## 5-1. Googleカレンダーを開く

パソコンでブラウザを開きます。
アドレスを入れるところに、次の文字を打ちます。

```
calendar.google.com
```

キーボードの `Enter` キーを1回押します。

## 5-2. 設定を開く

画面の**右上**に、歯車のマーク（⚙）があります。1回クリックします。

小さいメニューが出ます。「**設定**」を1回クリックします。

## 5-3. カレンダーを選ぶ

画面の**左側**に、メニューが縦に並んでいます。

「**マイカレンダーの設定**」という文字の下に、カレンダーの名前が並んでいます。
その中から「**ボカブキ**」を1回クリックします。

下にメニューが広がります。

## 5-4. URLをコピーする

画面を下にスクロールします。

「**カレンダーの統合**」という見出しがあります。

さらに下にスクロールすると、
「**非公開URL（iCal形式）**」または「**カレンダーの限定公開URL**」という項目があります。

その右に、四角が2枚重なったマーク（コピーのマーク）があります。1回クリックします。

「クリップボードにコピーしました」と出れば成功です。

## 5-5. メモ帳に貼る

パソコンで「メモ帳」（Macなら「テキストエディット」）を開きます。

`Ctrl` + `V`（Macなら `command` + `V`）を押します。

次のような長い文字が貼られます。

```
https://calendar.google.com/calendar/ical/xxxxx%40group.calendar.google.com/private-yyyyy/basic.ics
```

その行の**いちばん前**に、次の文字を足します。

```
ボカブキ|
```

こうなります。

```
ボカブキ|https://calendar.google.com/calendar/ical/xxxxx.../basic.ics
```

> `|` は「たてぼう」です。
> Windowsなら `Shift` を押しながら `¥` のキー。
> Macなら `Shift` を押しながら `¥` のキーです。

## 5-6. 残り2つも同じことをする

**5-3にもどります。**

画面の左側の「マイカレンダーの設定」から、今度は「**彼女**」を1回クリックします。

5-4と同じように、下にスクロールして「カレンダーの統合」を探し、
「非公開URL（iCal形式）」のコピーのマークを1回クリックします。

メモ帳を開いて、`Ctrl` + `V`（Macなら `command` + `V`）を押します。

その行のいちばん前に、次の文字を足します。

```
彼女|
```

**もう一度、5-3にもどります。**

画面の左側の「マイカレンダーの設定」から、今度は「**Motion**」を1回クリックします。

下にスクロールして「カレンダーの統合」を探し、
「非公開URL（iCal形式）」のコピーのマークを1回クリックします。

メモ帳を開いて、`Ctrl` + `V`（Macなら `command` + `V`）を押します。

その行のいちばん前に、次の文字を足します。

```
Motion|
```

## 5-7. 1行にまとめる

メモ帳に、3行あるはずです。

```
ボカブキ|https://...basic.ics
彼女|https://...basic.ics
Motion|https://...basic.ics
```

これを、**`,`（カンマ）でつないで1行にします。**
改行を消して、間にカンマを入れます。

```
ボカブキ|https://...basic.ics,彼女|https://...basic.ics,Motion|https://...basic.ics
```

**スペースは入れません。**

この1行を、あとで使います。メモ帳は開いたままにしておきます。

> **このURLの扱いについて**
> このURLを知っている人は、カレンダーの中身を見られます。
> 人に見せたり、Publicなリポジトリの中に直接書いたりしないでください。
> 次の章では、GitHubの「Secrets」という、中身が隠される場所に入れます。

---

# 第6章 自動更新をONにする

## 6-1. GitHubのリポジトリを開く

ブラウザで、次のアドレスを開きます。

```
github.com
```

キーボードの `Enter` キーを1回押します。

画面の左側に、リポジトリの一覧が出ています。`kyou` を1回クリックします。

## 6-2. Settingsを開く

ページの上のほうに、横並びのタブがあります。

```
Code   Issues   Pull requests   Actions   Projects   Security   Insights   Settings
```

いちばん右の「**Settings**」を1回クリックします。

## 6-3. Secretsの画面へ行く

画面の**左側**に、メニューが縦に並んでいます。

下のほうにスクロールして、「**Secrets and variables**」という文字を1回クリックします。

下に3つ出てきます。その中の「**Actions**」を1回クリックします。

## 6-4. カレンダーのURLを登録する

「**New repository secret**」という緑色のボタンがあります。1回クリックします。

「**Name**」という入力欄に、次の文字を打ちます。
**大文字と小文字、アンダーバーの位置まで、そのまま打ちます。**

```
ICS_URLS
```

その下の「**Secret**」という大きい入力欄に、
第5章の5-7で作った**1行**を貼ります。

（メモ帳でその行を選んで `Ctrl` + `C`、こちらの欄で `Ctrl` + `V`）

「**Add secret**」という緑色のボタンを1回クリックします。

## 6-5. できたことを確認する

一覧に `ICS_URLS` という文字が出ていれば成功です。
中身は `***` と表示されて見えません。それで正しいです。

## 6-6. Actionsを有効にする

ページの上のほうの横並びのタブから、「**Actions**」を1回クリックします。

「**I understand my workflows, go ahead and enable them**」という緑色のボタンが
出ていたら、1回クリックします。

出ていなければ、そのまま次に進みます。

## 6-7. 手で1回動かしてみる

画面の左側に「**build**」という文字があります。1回クリックします。

画面の右のほうに「**Run workflow**」というボタンがあります。1回クリックします。

小さいメニューが出ます。もう一度「**Run workflow**」という緑色のボタンを1回クリックします。

## 6-8. 待つ

10秒ほど待ってから、ブラウザの再読み込みをします。
（キーボードの `F5` キー、またはアドレスバーの左にある丸い矢印を1回クリック）

「build」という行が出てきます。

- **黄色い丸**が回っていたら、動いている最中です。1〜2分待ちます
- **緑のレ点**が付いたら成功です
- **赤い×**が付いたら失敗です。第10章を見てください

## 6-9. 結果を見る

ページの上のほうの横並びのタブから、「**Code**」を1回クリックします。

`data` というフォルダを1回クリックします。

中に `raw.json` と `events.json` という2つのファイルができていれば成功です。

`events.json` を1回クリックすると、中身が見られます。
今日と明日の予定が入っていれば、正しく動いています。

## 6-10. アプリで確認する

iPhoneで「きょう」のアイコンを1回タップします。

すでに開いていた場合は、いったんアプリを閉じてから開き直します。
（画面の下から上にスワイプして、「きょう」の画面を上に放り投げると閉じます）

カレンダーの予定が並んでいれば成功です。

**これで自動更新がONになりました。**
毎日 15時と20時に、勝手に更新されます。

---

# 第7章 Claudeを繋ぐ

**この章はできなくても大丈夫です。** できない場合、
1マスには「日付のいちばん近いタスク」がそのまま入ります。

やると、Claudeが予定とジャーナルを読んで
「今やるのはこれ1件」を選んでくれるようになります。

## 7-1. トークンを作る

パソコンで、Claude Code が入っている場合のみできます。

ターミナル（黒い画面）を開いて、次の文字を打ちます。

```
claude setup-token
```

キーボードの `Enter` キーを1回押します。

`sk-ant-oat01-` で始まる長い文字が出てきます。それを全部コピーします。

> Claude Code が入っていない場合は、この章は飛ばしてください。
> 第8章に進みます。

## 7-2. GitHubに登録する

ブラウザで `github.com` を開き、`kyou` を1回クリックします。

ページの上のほうの横並びのタブから、「**Settings**」を1回クリックします。

画面の左側のメニューを下にスクロールして、
「**Secrets and variables**」を1回クリックし、その下の「**Actions**」を1回クリックします。

「**New repository secret**」という緑色のボタンを1回クリックします。

「**Name**」の欄に、次の文字を打ちます。

```
CLAUDE_CODE_OAUTH_TOKEN
```

「**Secret**」の欄に、7-1でコピーした文字を貼ります。

「**Add secret**」という緑色のボタンを1回クリックします。

## 7-3. 動かしてみる

ページの上のほうの横並びのタブから、「**Actions**」を1回クリックします。

画面の左側の「**build**」を1回クリックします。

画面の右のほうの「**Run workflow**」を1回クリックし、
出てきた緑色の「**Run workflow**」をもう1回クリックします。

1〜2分待ってから、上のタブの「**Code**」→ `data` → `events.json` を開きます。

`"mas"` という項目に文字が入っていれば成功です。
空っぽ（`""`）なら、トークンが使えなかったということです。
その場合も予定は表示されるので、そのまま使って問題ありません。

---

# 第8章 ショートカットで「とい」を開く

決めた時間に、質問の画面が自動で開くようにします。

## 8-1. ショートカットアプリを開く

iPhoneのホーム画面で、「**ショートカット**」というアプリを探して、1回タップします。

見つからないときは、ホーム画面の真ん中あたりを指で下に引くと検索欄が出ます。
そこに `ショートカット` と打つと出てきます。

## 8-2. オートメーションの画面へ行く

画面のいちばん下に、「ショートカット」「オートメーション」という2つの文字があります。

「**オートメーション**」を1回タップします。

## 8-3. 新しく作る

画面の右上にある `＋` を1回タップします。

（すでに何か作ってある場合は、右上の `＋` です）

## 8-4. 時刻を選ぶ

一覧の中から「**時刻**」を1回タップします。

「時刻」の丸いボタンを1回タップして選びます。

その下に時間が出ています。1回タップして、`15:00` に合わせます。

その下に「毎日」「毎週」「毎月」があります。「**毎日**」を1回タップします。

さらに下に「**すぐに実行**」と「**実行前に尋ねる**」があります。
**「すぐに実行」を1回タップします。**

画面の右上の「**次へ**」を1回タップします。

## 8-5. 開くURLを決める

「新規空アクション」または検索欄が出ます。

検索欄に、次の文字を打ちます。

```
URLを開く
```

出てきた「**URLを開く**」を1回タップします。

「URL」と書かれた青い文字の部分を1回タップします。

そこに、第3章で出たアドレスの**うしろに `/?q` を足したもの**を打ちます。

```
https://kyou-xxx.pages.dev/?q
```

> **`/?q` が大事です。**
> これが付いていると「とい」の面が開きます。
> 付いていないと「きょう」の面が開きます。

画面の右上の「**完了**」を1回タップします。

## 8-6. もう1つ作る

**8-3にもどります。**

画面の右上にある `＋` を1回タップします。

一覧の中から「**時刻**」を1回タップします。

「時刻」の丸いボタンを1回タップして選びます。

その下の時間を1回タップして、今度は `20:00` に合わせます。

「**毎日**」を1回タップします。

「**すぐに実行**」を1回タップします。

画面の右上の「**次へ**」を1回タップします。

検索欄に `URLを開く` と打ちます。

出てきた「**URLを開く**」を1回タップします。

「URL」の青い文字を1回タップして、8-5と同じアドレスを打ちます。

```
https://kyou-xxx.pages.dev/?q
```

画面の右上の「**完了**」を1回タップします。

## 8-7. できたことを確認する

「オートメーション」の一覧に、2つ並んでいれば成功です。

**明日の15時に、勝手に質問の画面が開きます。**

---

# 第9章 答えたものをジャーナルに送る

「とい」で答えた内容と、1マスの完了を、
`journal` リポジトリに自動で足す仕組みです。

Cloudflare Workerを1本立てて、アプリの右上に設定を入れるだけです。
だいたい25分かかります。

「できた」を押したときに、Claudeがジャーナルとカレンダーを読み直して
**次の1マスを選び直す**のも、この章の仕組みです。

手順は別の紙にしてあります。`journal-ingest/SETUP.md` を開いてください。

**やらなくても、きょう/とい はそのまま使えます。**
設定しなければ何も送りません。1マスは1日2回の自動更新でだけ入れ替わります。

---

# 第10章 うまくいかないとき

## 画面が真っ白

- アドレスの打ち間違いがあります。第3章の3-7で出たアドレスを、もう一度確認します
- `https://` から全部打ちます

## 予定が出てこない

**まず確認すること**

第6章の6-9の手順で、`data/events.json` を開きます。

- **ファイルが無い** → 第6章の6-7から、もう一度やります
- **ファイルはあるが `"events": []` と空っぽ** → 今日と明日に予定が入っていないだけです。正常です
- **ファイルに予定が入っているのに画面に出ない** → iPhoneでアプリを完全に閉じてから開き直します

## Actionsが赤い×になる

「**Actions**」タブ → 「build」の赤い行を1回クリックします。

赤くなっている行を1回クリックすると、理由が出ます。

| 出ている文字 | 意味 | やること |
|---|---|---|
| `ICS_URLS が空` | Secretが登録されていない | 第6章の6-4をやり直す |
| `取得失敗` | URLが間違っている | 第5章をやり直す |
| `Bad credentials` | Claudeのトークンが使えない | 第7章は飛ばす。予定は出ます |

## 「とい」に切り替わらない

いちばん下の「とい ↓」の文字そのものをタップします。
その少し上の余白ではありません。

## 質問が3問で終わる

そういう作りです。もっとやりたいときは、
終わったあとに出る「**もう少しやる**」を1回タップします。

## 同じ質問ばかり出る

答えた言葉が、次の質問の材料になる作りです。
使っているうちに種類が増えます。

## 手で入れた予定が消えた

自動で取ってきた予定と、手で入れた予定は別に保存されています。
手で入れたものは3日過ぎると自動で消えます。

---

# 第11章 毎日の使いかた

## 朝（起きたあと）

15時にiPhoneが「とい」を開きます。

- 質問に答えて、真ん中の丸いボタンを1回タップすると送信されます
- 気分を付けたいときは、丸いボタンを**押したまま指をずらします**。上が😊、右が😢、下が😠、左が🤮です
- ずらしたまま**押し続ける**と、気分が強くなります
- **やめたいときは、指を真ん中に戻してから離します**。✕が出て、送信されません
- 何も書かずにタップすると「今はやらない」として送られます。それでいいです

## 夜（出かける前）

20時にもう一度開きます。同じように答えます。

## 予定を見たいとき

ホーム画面の「きょう」のアイコンを1回タップします。
今日と明日が出ます。

## 1マスが終わったとき

「**できた**」を1回タップします。

なにをやったかを書く欄が出ます。打って「**決定**」を1回タップします。
**打たなくても送れます。**

書いたものはジャーナルに残り、Claudeが次の1マスを選ぶ材料になります。

第9章の設置をしてあれば、そのまま「Claudeが選んでいます」に変わり、
2分から4分で次の1マスが出ます。**アプリを閉じても大丈夫です。**

していなければ、次の自動更新（15時か20時）まで待ちます。

**1マスは手では書き換えられません。** Claudeが決めます。
違うものをやりたいときは、「できた」のメモにそう書くと次に反映されます。

## 予定をすぐ取り込みたいとき

カレンダーに予定を足した直後など、自動更新（15時・20時）を待てないときです。

「**きょう**」の予定の見出しの右にある「**更新**」を1回タップします。

「更新中…」に変わります。**2分から4分**で、新しい予定が入ります。
アプリを閉じても大丈夫です。次に開いたときに反映されています。

1マスは変わることも変わらないこともあります。予定を取り直すのが目的なので、
Claudeが「まだこれが一番」と判断すれば同じままです。

第9章の設置をしていない場合、このボタンは押しても
「更新を頼めません」と出ます。

## 思いついたことを書き留めたいとき

「きょう」を開いて、いちばん下まで**指で上にスクロール**します。

「**メモ**」という欄があります。1回タップして、何か打ちます。

「**ジャーナルに送る**」を1回タップします。

書いたものはそのままジャーナルに入り、
Claudeが次の1マスを選ぶときの材料になります。

**途中で閉じても消えません。** 次に開くと続きから書けます。

## タスクを足したいとき

Googleカレンダーで、件名の**いちばん前**に `[タスク]` と付けて、
終日の予定として入れます。

例：

```
[タスク]垂れ幕の即日依頼を考える
```

これは予定の欄には出ません。1マスの候補と、質問の材料になります。
